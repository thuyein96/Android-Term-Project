<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Toway"
        tools:targetApi="31">

        <activity
            android:name=".activities.RoleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CustomerLoginActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".activities.CustomerRegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CustomerHomePageActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".activities.CallServiceActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".activities.DriverLoginActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".activities.DriverRegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DriverHomePageActivity"
            android:exported="false">
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_key"/>
    </application>

</manifest>